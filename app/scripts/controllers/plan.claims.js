/**
 * @ngdoc controller
 * @name PlanClaims
 *
 * @description
 * Displays claims that the user has filed for repairing or selling their device
 * 
 */

export class PlanClaims {
	constructor(activitiesData) {
		this.activities = activitiesData
	}
}